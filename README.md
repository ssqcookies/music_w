
# React（React19 +TS+ Vite8）复习之云音乐项目



## 一 项目简介

  该项目使用react+ts仿网易云，对知识进行巩固复习。(依据codewhy)

###关于数据：

  页面展示数据来自网易云开放平台API文档，自行将数据进行拼凑，形成json数据，放在public文件夹下。
  本地开发时直接读取 data/xxxx.json 文件，而生产环境（Vercel）走 /api/xxxx 接口。
  其中，音乐播放数据是在网上下载的mp3文件，放在本地目录下。
  并未直接调取网易云开放平台的数据，后续考虑接入，这样数据更加真实，且不会给别人造成困扰。



###项目已经完成功能如下：
  1.推荐页面：

  * 轮播图
  * 热门推荐
  * 新碟上架
  * 榜单
  * 等等

  2.歌曲播放：

  * 目前做了榜单中歌曲的点击播放；
  * 其他页面只要将歌曲的id传入到redux中就可以（由于没有真正接入api，点击后也是播放的默认本地歌曲）；
  * 做了歌曲的各种控制（暂停、播放、上一首、下一首、进度改变）；
  * 做了播放循序切换：顺序播放、随机播放、单曲循环；
  * 做了歌词的解析、展示、滚动；


## 二 项目规范

**项目规范：项目中有一些开发规范和代码风格**

* 1.文件夹、文件名称统一小写、多个单词以连接符（-）连接；
* 2.JavaScript变量名称采用小驼峰标识，常量全部使用大写字母，组件采用大驼峰；
* 3.CSS采用普通CSS和styled-component结合来编写（全局采用普通CSS、局部采用styled-component）;
* 4.整个项目不再使用class组件，统一使用函数式组件，并且全面使用Hooks；
* 5.所有的函数式组件，为了避免不必要的渲染，全部使用memo进行包裹；
* 6.组件内部的状态，使用useState、useReducer；业务数据全部放在redux中管理；
* 7.函数组件内部基本按照如下顺序编写代码：
  * 组件内部state管理；
  * redux的hooks代码；
  * 其他组件hooks代码；
  * 其他逻辑代码；
  * 返回JSX代码；
* 8.redux代码规范如下：
  * redux结合ImmutableJS
  * 每个模块有自己独立的reducer，通过combineReducer进行合并；
  * 异步请求代码使用redux-thunk，并且写在actionCreators中；
  * redux直接采用redux hooks方式编写，不再使用connect；
* 9.网络请求采用axios
  * 对axios进行二次封装；
  * 所有的模块请求会放到一个请求文件中单独管理；
* 10.项目使用AntDesign
  * 项目中某些AntDesign中的组件会被拿过来使用；
  * 但是多部分组件还是自己进行编写；
* 其他规范在项目中根据实际情况决定和编写；

## 三、后续计划

将其他页面补充进去

1.发现音乐——排行榜页面：
* 各种榜单的切换；


2.发现音乐——歌单页面
* 选择分类、选择分类后根据分类切换歌单；
* 根据分类，歌单列表的展示；
* 分页功能；


3.发现音乐——主播电台：
* 电台分类的展示、滚动；
* 不同分类展示不同的数据；
* 电台排行榜展示、分页；


4.发现音乐——歌手页面：
* 各种歌手分类（没找到接口，还自定义了一些数据）
* 歌手字母分类、对应歌手展示；


5.发现音乐——新碟上架页面：
* 热门新碟；
* 全部新碟、分页展示；


## 四、项目运行


安装项目依赖：

```shell
npm install
```

项目运行：

```shell
npm run dev
```



